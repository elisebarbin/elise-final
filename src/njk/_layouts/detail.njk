{% extends '_layouts/base.njk' %}

{% block page_title %} {{ members.name }}{% endblock %}

{% block content %}
    <article class="container">
    <div class="row">
    </div>

    {% block story %}
    {% endblock %}
    {% block info %}
      <div class="detail-top col-md-12">
      <img class="detail-img" src="assets/img/members/{{members.image}}">
      <h1 class= "detail-name">{{ members.name }}</h1>
      <h3 class="detail-details">{{members.instruments}} {{members.tenure}}</h3>
      </div>

    <div class="col-sm-12">
      <p>{{ members.text | safe}}</p>

    {% endblock %}

    </div>
    </div>
    </article>
    {% endblock %}

 
